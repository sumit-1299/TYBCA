import 'package:flutter/material.dart';

class CnB extends StatelessWidget {
  const CnB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Cash & Bank")
      ),
    );
  }
}
